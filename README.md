# Daily-wages-project
This project is a platform-based application that includes three types of users: admin, user, and worker. The frontend is developed using React with Tailwind CSS for styling. The backend is built with Node.js and Express. The database used is MongoDB, and the entire project is written in TypeScript.
